# Changelog

## v0.3.0 (2026-05-07)

### 弹窗体验优化
- 导航按钮生命周期与弹窗同步，消除视觉延迟
- 自定义粉紫主题 Loading 覆盖层（SVG），替代 Element Plus 蓝色 default
- 弹窗内容不再销毁重建，消除「内容→加载中→内容」闪烁

### 用户交互增强
- 修改用户名：实时 blur 校验 + 唯一性检测，无需确认字段
- 修改密码：新密码/确认密码 inline 实时校验（长度 + 一致性）
- 修改密码成功后强制重新登录，清除旧 Token
- 登录密码错误时正确显示「用户名或密码错误」而非「请先登录」
- 修复密码修改 204 No Content 导致的前端 JSON 解析报错

### 表单与文案
- 编辑表单：标签与简介位置互换（标签在上、简介在下）
- 标签输入提示改为「中、英文逗号分隔」
- 全部动态列表 0 分显示「暂不打分」
- 页面上所有「暂未观看」文字统一替换为「暂不打分」

### 后端优化
- Bangumi 集成：V0 API + 旧 API 双通道获取简介，单次调用 + 缓存
- 新增 `/auth/check-username` 端点用于前端实时校验
- 新增 `/auth/me/password` 修改密码端点
- 优化番剧详情查询性能

### 其他
- 修改用户名后自动刷新「大家在看啥」等列表的用户名显示
- 全项目中英文逗号兼容处理（split /[,，]/）
- API 请求追加实时时间戳，防止浏览器/代理缓存

---

## [0.2.0] — 2026-05-12

### ✨ 新增功能

#### 番剧列表增强
- **类型标签显示**：番剧名后拼接粉紫风格 Tag，展示前 3 个类型标签，点击标签自动填入筛选输入框
- **标签筛选**：点击标签可筛选同类型番剧，手动输入搜索框时自动清除标签筛选
- **按推荐排序**：番剧列表增加「按推荐」排序选项
- **清除搜索**：搜索输入框支持一键清空按钮

#### 评分展示增强
- **平均分卡片**：番剧详情弹窗中新增「均分」和「推荐度」两张玻璃卡，大字显示分数
- **文字评分**：大家的评分/大家在看啥/全部动态中的评分推荐改为文字展示，⭐粉 🏅紫双色区分

#### 用户体验
- **回车提交**：登录/注册弹窗支持 Enter 键提交
- **整卡点击评分**：我的评分模块整块可点击进入评分模式
- **分页默认 50 条**：番剧列表后端分页优化

### 🎨 UI/UX 优化

#### 视觉升级
- **装饰线加粗**：三大模块标题装饰线 2px→6px，间距加大至 12px
- **全局滚动条**：淡粉偏白渐变细条（6px），弹窗内隐藏滚动条保持清爽
- **弹窗高度自适应**：添加番剧/登录注册弹窗去掉固定 90vh，内容决定高度
- **Select 焦点颜色**：el-select focus 状态保证粉色，无蓝色残留
- **Rate 组件色彩**：全局 rate 从粉色到紫色渐变，默认星色覆盖为粉色
- **图标颜色统一**：⭐score → 粉色，🏅recommend → 紫色，`gap-1`(4px) 间距
- **按钮等宽**：保存评分/取消按钮各占 50%宽度
- **未评高亮行**：未评番剧行从左至右粉白渐变，更柔和
- **封面放大**：大家在看啥/全部动态封面 40×56 → 56×80
- **Favicon**：更新为粉紫渐变星星图标

#### 布局修复
- **弹窗防位移**：`overflow-y:scroll` → `scrollbar-gutter:stable`，弹窗弹出时首页无偏移
- **评论换行**：大家在看啥评论另起一行，`whitespace-pre-wrap` 支持多段显示
- **全部动态**：去掉时间显示，评论完整显示不截断

### 🐛 问题修复

- **标签搜索不生效**：`filterByTag` 同时传 `search+tag` 参数导致交集为空，修复为有 `tagFilter` 时不传 `search`
- **按推荐排序**：后端排序逻辑缺失 `avg_rec` case
- **搜索类型搜索**：搜索框只对标题和日文名生效（不搜索标签 JSON）
- **相邻按钮左边距**：Element Plus 默认 `.el-button + .el-button { margin-left:12px }` 导致取消按钮偏移，全局覆盖
- **弹窗滚动条**：弹窗打开时首页滚动条不占位导致的布局偏移

### 🔧 技术改进

- **邀请码管理**：`manage_codes.py` 更新默认码列表
- **排序扩展**：后端 API 新增 `avg_rec` 排序支持
- **标签搜索**：后端 `tag` 参数通过 `Anime.tags.contains()` 过滤
- **前端状态**：新增 `tagFilter` 响应式状态，区分手动输入和标签点击

---

## [0.1.0] — 2026-05-06

### ✨ 新增功能

#### 番剧系统
- **番剧列表**：首页展示所有番剧卡片，含封面、标题、均分、评分人数
- **番剧详情弹窗**：点击卡片弹出，含封面、简介、状态、标签信息
- **编辑番剧信息**：已登录用户可在详情弹窗中编辑番剧字段
- **删除番剧**：已登录用户可删除番剧（含确认弹窗）
- **添加番剧**：支持 Bangumi 搜索导入 + 手动填写两种方式
- **随机推荐**：首页「今天看什么」按钮，随机推荐未评分的番剧

#### 评分系统
- **评分评分**：创建/更新评分，含番剧评分（10星）+ 补番推荐度（10星）+ 文本评论
- **评分展示**：详情弹窗中展示「我的评分」和「大家的评分」
- **大家的评分卡片**：每条评分为独立卡片，hover 有动效，清晰区分用户
- **评分历史**：个人评分历史弹窗，含番剧封面、评分、时间

#### 用户系统
- **注册/登录**：基于邀请码的注册系统 + JWT 登录
- **按需登录**：浏览无需登录，评分/添加番剧时触发登录弹窗

#### Bangumi 集成
- **番剧搜索**：输入关键词实时搜索 Bangumi 数据库
- **番剧详情导入**：选择搜索结果后异步获取完整信息（状态、季度等）
- **Infobox 解析**：从 Bangumi 页面 HTML 中提取放送日期、季度、集数

### 🎨 UI/UX 优化

- **主题系统**：粉紫渐变品牌色，覆盖 Element Plus 所有组件（按钮/输入框/选择器/评分等）
- **毛玻璃卡片**：首页番剧卡片和评分卡片采用毛玻璃风格
- **弹窗固定高度**：详情弹窗和添加弹窗高度固定为 90vh，超出内容在弹窗内部滚动
- **弹窗居中**：弹窗始终在视口中垂直水平居中
- **智能渐变色**：详情弹窗背景色根据封面主色动态取色
- **导航按钮**：番剧详情弹窗两侧的上/下一条导航按钮
- **按钮系统**：粉紫渐变主按钮 / 白底细紫边框次要按钮 / 文字按钮
- **标题样式**：粉紫渐变色标题 + 竖线装饰
- **全局滚动条**：横向滚动（标签分类）隐藏滚动条，保持美观
- **回到顶部**：右下角回到顶部悬浮按钮

### 🐛 问题修复

- **弹窗高度不生效**：`class="anime-dialog"` 实际落在 `.el-dialog` 元素上（非 overlay），CSS 选择器从 `.anime-dialog .el-dialog` 修正为 `.anime-dialog`
- **`overlay-class` 不被 Element Plus 识别**：改用 `:has()` CSS 选择器实现弹窗居中
- **弹窗被默认 margin 推偏**：去除 Element Plus 默认的 `margin: 15vh auto 50px`
- **输入框高度压缩**：编辑/添加表单 `size="small"` → `size="default"`
- **添加番剧表单间距过大**：自定义 FormItem 间距压缩至 8px
- **导航按钮遮挡**：使用 `<Teleport to="body">` 提升 z-index
- **弹窗内部不滚动**：`flex: 1; overflow-y: auto` 配合固定高度

### 📦 数据

- **模拟用户**：6 个不同风格的测试用户（alice / bob / carol / david / erika / felix）
- **模拟评分**：78 条评分，覆盖 22 部番剧
- **评分多样性**：含短评（1-2句）、中评、长评（多段）、结构化评分（含优缺点分析）

### 🔧 技术改进

- **后端**：新增 Bangumi 详情接口、番剧删除接口、评分历史接口、Infobox HTML 解析服务
- **前端**：新增评分历史弹窗组件、TypeScript 类型扩展、API 封装方法
- **认证**：按需登录模式，未登录评分触发 AuthDialog
- **去重**：添加番剧时的 title_cn 去重校验

### 📖 文档

- **README.md**：更新 API 端点文档、测试账号信息、项目结构
- **product-design.md**：完整产品设计文档（V4.5·按需登录版）
